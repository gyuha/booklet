# booklet

macOS용 개인 epub 뷰어 (Tauri v2 + vanilla TS + 벤더링한 foliate-js).

## "배포" 트리거

사용자가 **`배포`** 라고만 입력하면 다음을 실행한다:

```
task deploy            # patch (0.1.0 → 0.1.1) — 기본
task deploy -- minor   # "배포 minor"
task deploy -- major   # "배포 major"
```

스크립트(`scripts/deploy.sh`)가 순서대로 한다: 사전 점검 → `task check` → 버전 3파일
동기화 → 릴리스 빌드 → `ditto` zip → 커밋·태그 → 푸시 → `gh release create`.

**지켜야 할 것:**

- **거부되면 거부된 이유를 그대로 보고하고 멈춘다.** 스크립트는 게이트를 통과하지 못하면
  아무것도 건드리지 않는다(비파괴). 특히 **커밋되지 않은 변경이 있으면 거부**하는데,
  이때 **대신 커밋해 주지 마라** — 무엇을 커밋할지는 사용자의 판단이다. 상태를 보여주고 물어라.
- 체크가 실패하면 배포하지 않는다. 배포는 green 상태에서만 한다.
- 버전은 `package.json` · `src-tauri/tauri.conf.json` · `src-tauri/Cargo.toml`
  **세 곳**에 있다. 손으로 고치지 말고 스크립트에 맡긴다.

## 이 프로젝트의 배포 전제

- **대상은 본인과 본인의 다른 맥이다.** 서명 인증서가 없으므로(`security find-identity` → 0개)
  다운로드한 앱은 quarantine 때문에 열리지 않는다. 릴리스 노트에 `xattr -dr` 한 줄이
  자동으로 들어간다. **다른 사람에게 배포하려면** Developer ID + 공증(연 $99)이 사실상
  필수이고, 그건 별도 작업이다 — 임의로 CI·서명 파이프라인을 만들지 마라.
- `bundle.targets` 는 `["app"]` 이다. **Tauri 의 DMG 번들러는 되살리지 마라** —
  `bundle_dmg.sh` 가 Finder 자동화에 의존해 플레이키하고, 실패하면 마운트된 볼륨을 남긴다.
- 배포용 DMG 는 `scripts/make-dmg.sh` 가 **`hdiutil` 만으로** 굽는다(`task build` 가 빌드
  직후 호출). Finder 자동화가 없고 `-srcfolder` 는 볼륨을 마운트하지 않으므로 위 실패 모드가
  구조적으로 없다 — 대가는 창 배경·아이콘 좌표가 없다는 것뿐이다. 릴리스 산출물은 이
  DMG 하나이며 zip 은 만들지 않는다.

## 검증

`task check` 가 스톱 조건 C1–C9 를 전부 돌린다. 개별 실행은 `task check:c3`.
**체크 세트의 설계 원칙과 관측 한계는 `scripts/checks/README.md` 에 있다 — 체크를
건드리기 전에 읽어라.** 요지 세 줄:

1. 정적 스냅샷이 아니라 **조작 시퀀스**를 검증한다(클릭한 뒤 키를 누른다, 책을 열어둔 채 또 연다).
2. 새 체크는 **일부러 깨뜨려 실패를 확인한 뒤** 채택한다.
3. **체크를 느슨하게 고쳐 통과시키지 않는다.** 임계값 낮추기·단언 삭제·샘플 교체 금지.

## 반복해서 밟은 함정

- **입력 이벤트는 섹션 iframe 에도 붙여야 한다.** 본문은 섹션마다 별도 iframe 이라
  최상위 `document` 에만 붙이면 커서·포커스가 본문으로 간 뒤 조용히 죽는다.
  키보드와 휠에서 각각 한 번씩 겪었다.
- **경로는 NFC 로 정규화한다.** macOS 는 NFD 로 넘겨주므로, 정규화 없이 경로를 키로 쓰면
  같은 책이 두 항목으로 저장되어 위치 복원이 조용히 실패한다(한국어 파일명에서 항상).
- **벤더링한 foliate-js 를 먼저 읽어라.** 키 배선·`view.close()`·이미지 크기 제한 모두
  답이 upstream 에 있었는데 읽지 않고 배선해서 버그를 만들었다. 소스는 수정하지 않는다.
- **추측보다 계측이 빠르다.** 렌더링·레이아웃 문제는 프로브를 짜서 실제 값을 찍는 편이
  CSS 규칙으로 추론하는 것보다 매번 빨랐다. 음성 결과에는 반드시 대조군을 세운다.
